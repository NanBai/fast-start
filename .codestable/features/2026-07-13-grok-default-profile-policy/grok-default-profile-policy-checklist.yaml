feature: 2026-07-13-grok-default-profile-policy
created: 2026-07-13

dod:
  commands:
    - id: CMD-001
      command: "cd src-tauri && cargo test --lib"
      core: true
      failure_handling: fix-or-block
  evidence_required: [command_output, diff_summary]
  cleanliness:
    debug_output: forbidden

steps:
  - action: "ensure_default_profile 按 models_base_url 决定 active"
    exit_signal: "单测 OAuth inactive / API active / skip existing"
    status: done

checks:
  - item: "无 API 上游 Default 不 active"
    source: 验收场景
    status: passed
  - item: "有 models_base_url 则 active"
    source: 验收场景
    status: passed
